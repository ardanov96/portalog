# Database — ganti USER, PASSWORD, DATABASE sesuai PostgreSQL lokal kamu
DATABASE_URL="postgresql://postgres:password@localhost:5432/forwarder_saas"

# Auth — generate dengan: openssl rand -base64 32
AUTH_SECRET="ganti-dengan-random-string-minimal-32-karakter"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Email notifikasi (opsional, isi nanti)
RESEND_API_KEY=""
EMAIL_FROM="noreply@yourdomain.com"

# WhatsApp notifikasi via Fonnte (opsional)
FONNTE_API_KEY=""
FONNTE_SENDER_PHONE=""
