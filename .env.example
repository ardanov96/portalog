# Database 
DATABASE_URL="postgresql://postgres:desmone8327@localhost:5432/db_portalog"

# Auth — generate dengan: openssl rand -base64 32
AUTH_SECRET="ganti-dengan-random-string-minimal-32-karakter"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Email notifikasi (opsional, isi nanti)
EMAIL_USER="ardhakrs@gmail.com"
EMAIL_PASS="hvnwyohukzyjlcam"   # Gmail App Password (16 karakter)
EMAIL_FROM="Portalog <ardhakrs@gmail.com>"

# WhatsApp notifikasi via Fonnte (opsional)
FONNTE_API_KEY="WLCJKm3VEhAj8cjHSSWW"
FONNTE_SENDER_PHONE="62895634647599"

# ─── Email via Resend ─────────────────────────────────────────────────────────
# Setup: resend.com → Create API Key → Copy ke sini
# Verifikasi domain untuk kirim dari domain sendiri
# Tanpa domain: pakai EMAIL_FROM="onboarding@resend.dev" (hanya untuk testing)
# Gratis: 3.000 email/bulan
RESEND_API_KEY="re_3RHZoCtg_Cm98ozi9G1bQFMtC1ZHeyKGB"
EMAIL_FROM="onboarding@resend.dev"

# ─── Anthropic Claude API (untuk fitur AI: HS Code suggest, dll) ─────────────
# Dapatkan API key di: console.anthropic.com
ANTHROPIC_API_KEY="sk-ant-xxxxxxxxxx"
# Setup: dash.cloudflare.com → R2 → Create bucket "forwarder-docs"
# API token: R2 → Manage API Tokens → Create (Object Read & Write)
# Endpoint: R2 bucket settings → S3 API endpoint
# Kalau tidak diisi, file disimpan lokal di /public/uploads (dev only)
STORAGE_ENDPOINT="https://<ACCOUNT_ID>.r2.cloudflarestorage.com"
STORAGE_ACCESS_KEY=""
STORAGE_SECRET_KEY=""
STORAGE_BUCKET="forwarder-docs"
# Public URL — aktifkan "Public Access" di R2 bucket settings
STORAGE_PUBLIC_URL=""

# ─── Cron Jobs (Vercel Cron) ──────────────────────────────────────────────────
# Generate: openssl rand -hex 32
# Tambahkan juga ke Vercel Dashboard → Settings → Environment Variables
CRON_SECRET="bffec0a455c8aed681ebc7d650c6bb042ae4a5dbe212ff87a93c8b2875a1f32d"

# ─── BetterStack Error Monitoring ─────────────────────────────────────────────
BETTERSTACK_TOKEN="yqWQhXR4i41xdGV262Dk4ShJ"

# ─── Vessel Tracking API ──────────────────────────────────────────────────────
# Pilih salah satu provider (atau kombinasi — sistem otomatis fallback):
#
# VesselFinder (recommended, freemium, 100 req/hari gratis)
# Daftar: https://apidocs.vesselfinder.com
VESSEL_FINDER_API_KEY=""
#
# MarineTraffic (enterprise, per-credit)
# https://www.marinetraffic.com/en/p/api-services
MARINE_TRAFFIC_API_KEY=""
#
# MyShipTracking (alternatif terjangkau)
# https://www.myshiptracking.com/developers
MY_SHIP_TRACKING_API_KEY=""
#
# Tanpa API key → mode simulasi aktif (untuk development)

# Midtrans Payment Gateway
MIDTRANS_SERVER_KEY=SB-Mid-server-xxxxxxxx   # dari dashboard Midtrans
MIDTRANS_CLIENT_KEY=SB-Mid-client-xxxxxxxx
MIDTRANS_IS_PRODUCTION=false                 # ganti true saat go-live
NEXT_PUBLIC_MIDTRANS_CLIENT_KEY=SB-Mid-client-xxxxxxxx
NEXT_PUBLIC_MIDTRANS_IS_PRODUCTION=false
